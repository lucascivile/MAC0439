package bd.relacional;

import java.util.ArrayList;

import modelo.relacional.Acordo;

public class AcordoDAO {

    // private Connection connection;

    public AcordoDAO() {
		// this.connection = ConnectionFactory.getInstance().getConnection();
    }

    /**
    * Cria o acordo no banco.
    * Retorna o id do acordo inserido.
    */
    public int insert(Acordo acordo) {
        return 0;
    }

    /**
    * Retorna um ArrayList de todos os acordos existentes no banco.
    */
    public ArrayList<Acordo> list() {
      return null;
    }
}
