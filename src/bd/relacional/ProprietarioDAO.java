package bd.relacional;

import modelo.relacional.Proprietario;

public class ProprietarioDAO {
    
    // private Connection connection;

    public ProprietarioDAO() {
		// this.connection = ConnectionFactory.getInstance().getConnection();
    }

    /**
    * Cria o proprietário no banco.
    */
    public void insert(Proprietario usuario) {
    }

    /**
    * Atualiza o proprietário no banco.
    */
    public void update(Proprietario usuario) {
    }

    /**
    * Retorna o proprietário com o cpf fornecido.
    */
    public Proprietario get(String cpf) {
        return null;
    }
}
